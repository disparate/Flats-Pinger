<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="SubwaysSelectorView">
        <attr name="selectedColor" format="color" />
        <attr name="checkboxSelector" format="integer" />
    </declare-styleable>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>


    <declare-styleable name="ContentViewFlipper">
        <attr name="content_empty_text" format="string" />
        <attr name="content_loading_progress" format="reference" />
        <attr name="progress_auto_start" format="boolean" />
    </declare-styleable>
</resources>